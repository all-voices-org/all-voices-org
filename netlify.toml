[build]
    command = "npm run build"
    publish = "dist"

# Redirect root to English locale by default
[[redirects]]
    from = "/"
    to = "/en/"
    status = 302

# Handle admin identity authentication
[[redirects]]
    from = "/admin/*"
    to = "/admin/index.html"
    status = 200
